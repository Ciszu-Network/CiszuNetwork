info:
  name: MuzicMania web
  type: http
  seq: 3

http:
  method: GET
  url: https://muzicmania.vercel.app

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 200", function() {
          expect(res.status).to.equal(200);
        });
