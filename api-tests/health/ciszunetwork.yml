info:
  name: CiszuNetwork web
  type: http
  seq: 1

http:
  method: GET
  url: https://ciszunetwork.vercel.app

runtime:
  scripts:
    - type: tests
      code: |-
        test("returns 200", function() {
          expect(res.status).to.equal(200);
        });
